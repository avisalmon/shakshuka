# Shakshuka

AutoAgent-powered project.

## Quick Start
1. Open in VS Code
2. Write your task in `program.md`
3. Run `@autoagent /run` in Copilot Chat

## Structure
- `program.md` — AutoAgent task definition
- `state/` — runtime knowledge and plans
- `autoagent.config.json` — iteration and tool config
